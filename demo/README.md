This directory contains a [VHS](https://github.com/charmbracelet/vhs) tape used to record a demo of `lnks`.

You can record a new demo from the root directory of this repo via:

```bash
vhs < demo/demo.tape
```
